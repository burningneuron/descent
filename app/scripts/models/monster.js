/*global define*/

define([
    'underscore',
    'backbone'
], function (_, Backbone) {
    'use strict';

    var MonsterModel = Backbone.Model.extend({
        defaults: {
        }
    });

    return MonsterModel;
});
